// Vendored class map for the Cloudscape internal dropdown-status (the
// loading/error footer in select/multiselect/autosuggest dropdowns), so the
// test-utils find it (findStatusIndicator=.root, findErrorRecoveryButton=.recovery).
export default {
  root: "awsui_root_vrgzu_2pqh1_145",
  recovery: "awsui_recovery_vrgzu_2pqh1_180",
};
