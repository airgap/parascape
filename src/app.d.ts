// `.pui` (Para UI) component imports are typed by the ambient that
// @lyku/para-preprocess ships. One reference line — no hand-written
// `declare module '*.pui'` with a guessed component type.
/// <reference types="@lyku/para-preprocess/pui" />
